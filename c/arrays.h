#ifndef ARRAYS_H
#define ARRAYS_H

#include <stdbool.h>

// Definición del struct arreglo_t

// Declaración de funciones
void sortArray(int* arr, int size);
void llenarAray(arreglo_t* arr_struct);

#endif // ARRAYS_H
